จงเขียนฟังก์ชันที่ชื่อว่า calculateString() ที่ return 0; 
เพื่อคำนวณผลลัพธ์ที่อยู่ใน String text แล้ว return ออกมาเป็น int 
String text ที่ว่าจะเป็นการคำนวณของตัวเลขแค่ 2 ตัว(1คู่) เท่านั้น 
เช่น 3 + 4 การคำนวณนี้จะมีแต่ + - * ไม่มีหาร ไม่ต้องเช็คกรณีนี้ และให้ return ออกมาเป็น int ได้เลย ไม่ต้อง for loop
1)การตัดคำสำหรับตัวแปร String text โดยใช้ช่องว่าง แล้วให้ผลลัพธ์ออกมาเป็น String array ให้ใช้คำสั่ง text.split(“ “)
2)การตรวจสอบว่า String a มีค่าที่บรรจุอยู่เท่ากันกับ b หรือไม่ ให้ใช้คำสั่ง a.equals(b) อย่าใช้ == ผลลัพธ์จะเป็น Boolean 
3)การแปลงชนิดของตัวแปร String a ให้กลายเป็น int ให้ใช้คำสั่ง Integer.parseInt(a)